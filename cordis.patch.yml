# DeepSeek whale countdown widget: persistent corner clock for the harness GUI.
# Composes the package so the client-modules service serves its browser bundle.
- insert:
    - id: dsh-whale-clock
      name: 'dsh-whale-clock'
