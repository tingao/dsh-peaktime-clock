# DeepSeek peak-time whale countdown widget: persistent corner clock for the harness GUI.
# Composes the package so the client-modules service serves its browser bundle.
- insert:
    - id: dsh-peaktime-clock
      name: 'dsh-peaktime-clock'
